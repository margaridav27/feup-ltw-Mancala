class Board {
    constructor(board, seeds, holes) {
        this.board = board;
        this.seeds = seeds;
        this.holes = holes;
    }

    /* constructs board based on number of holes and seeds on each hole */
    constructBoard() {
    }
    
    /* retrieves board to be displayed properly */
    retrieveBoard() { 
        /* should we just return the array or render it here to document html? */
    }

}